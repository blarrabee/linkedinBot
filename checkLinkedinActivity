function checkUserActivity() {
  if (active.classList[2] === true){
    console.log("user is active");
    console.log(dateTime); }
  else {
    console.log("user is not active") }
};


if (active.classList[2] === true){
  console.log("user is active") }
else {
  console.log("user is not active") }

  function mainCheck() {
    checkCamilleActivity();

    setTimeout(mainCheck, 5000);
  }

function getDate() {
var newDate = new Date();
var datetime = "LastSync: " + newDate.today() + " @ " + newDate.timeNow();
console.log(newDate);
}
  var newDate = new Date();
  var datetime = "LastSync: " + newDate.today() + " @ " + newDate.timeNow();


//find green dot element
var active = document.getElementById('ember28837'); console.log(active.attribute);

function timeCheck() {
var newDate = new Date();
var datetime = "LastSync: " + newDate.today() + " @ " + newDate.timeNow();

  setTimeout(mainCheck, 5000);
}
